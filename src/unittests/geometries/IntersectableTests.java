package unittests.geometries;

import org.junit.jupiter.api.Test;
import primitives.*;
import geometries.*;
import java.util.List;


import static org.junit.jupiter.api.Assertions.*;

class IntersectableTests {

    /*
    @Test
    void testFilterIntersections() {
        List<Intersectable.GeoPoint> lst = List.of(new Intersectable.GeoPoint(new Sphere(new Point(0,0,0),1d), new Point(0,0,1)));
        Ray ray = new Ray(new Point(0,0,10), new Vector(0,0,-1));
        assertNull(Intersectable.filterIntersections(lst, ray, 2d));
        assertEquals(lst, Intersectable.filterIntersections(lst, ray, Double.POSITIVE_INFINITY));

    }*/
}